Universe            = Vanilla
Executable          = pythonwrapper.bat

{% for batch_dir in batches %}
Arguments = {{ batch_dir }} focusStepsPelmo.pelmo.runner -p {{ batch_dir }} -w {{ batch_dir }}-work -c{% for crop in crops %} {{ crop.name }}{% endfor %} -s{% for scenario in scenarios %} {{ scenario.name }}{% endfor %} --logfile {{batch_dir}}-log.json --loglevel {{ loglevel }} -o {{ batch_dir }}-output.json
transfer_input_files = common.zip,{{ batch_dir }}.zip,requirements.txt
transfer_output_files = {{ batch_dir }}-output.json,{{ batch_dir}}-log.json
Queue
{% endfor %}